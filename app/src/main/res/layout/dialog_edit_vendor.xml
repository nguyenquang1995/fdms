<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="vendor"
            type="com.framgia.fdms.data.model.Producer"
        />
        <variable
            name="viewModel"
            type="com.framgia.fdms.screen.producer.vendor.VendorViewModel"
            />
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/dp_15"
    >
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/dp_15"
            android:layout_marginLeft="@dimen/dp_2"
            android:text="@string/action_edit"
            android:textColor="@color/color_black"
            android:textSize="@dimen/sp_20"
        />
        <android.support.design.widget.TextInputLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:messageError='@{viewModel.messageError}'
        >

            <EditText
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/title_full_name"
                android:text='@={vendor.name}'
            />

        </android.support.design.widget.TextInputLayout>
        <android.support.design.widget.TextInputLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
        >
            <EditText
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/title_description"
                android:text='@={vendor.description}'
            />
        </android.support.design.widget.TextInputLayout>
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
        >
            <Button
                android:layout_width="@dimen/dp_70"
                android:layout_height="@dimen/dp_50"
                android:background="@color/color_white"
                android:onClick="@{() -> viewModel.onEditCancelClick(vendor)}"
                android:text="@string/action_cancel"
                android:textColor="@color/colorAccent"
            />
            <Button
                android:layout_width="@dimen/dp_70"
                android:layout_height="@dimen/dp_50"
                android:background="@color/color_white"
                android:onClick="@{() -> viewModel.onEditSubmitClick(vendor)}"
                android:text="@string/title_ok"
                android:textColor="@color/colorAccent"
            />
        </LinearLayout>

    </LinearLayout>
</layout>

